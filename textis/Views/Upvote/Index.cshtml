@model IEnumerable<textis.ViewModel.UpvoteViewModel>

@{
    ViewBag.Title = "Yfirlit";
}

<h2>Yfirlit</h2>

<p>
    @Html.ActionLink("Stofna nýtt", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Notandi
        </th>
        <th>
            Dagsetning
        </th>
        <th>
            Myndefni
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectName)
        </td>
        <td>
            @Html.ActionLink("Breyta", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Nánar", "Details", new { id=item.Id }) |
            @Html.ActionLink("Eyða", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
