@model IEnumerable<textis.ViewModel.ProjectLineViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Notandi
        </th>
        <th>
            Tími frá
        </th>
        <th>
            Tími til
        </th>
        <th>
            Lína 1
        </th>
        <th>
            Lína 2
        </th>
        <th>
            Dagsetning
        </th>
        <th>
            Tungumál
        </th>
        <th>
            Myndefni
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeFrom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeTo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TextLine1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TextLine2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Language)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjectName)
        </td>
        <td>
            @Html.ActionLink("Breyta", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Nánar", "Details", new { id=item.Id }) |
            @Html.ActionLink("Eyða", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
